[pytest]
# Le projet n'est pas packagé (pas de setup.py/pyproject) : app.py, db_manager.py,
# kavita_api.py, etc. sont importés comme modules top-level. On ajoute donc la
# racine du dépôt au sys.path de pytest plutôt que d'installer le projet.
pythonpath = . tests
testpaths = tests
